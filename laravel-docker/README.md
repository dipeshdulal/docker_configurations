Docker configuration for Laravel with `mysql`, `phpmyadmin`, `nginx`, `php` and other php extensions

Folder structure:
```
laravel_installation
    |- laravel-docker (configuration-folder)
    |- app
    |- database
    |- routes
    ...
```
